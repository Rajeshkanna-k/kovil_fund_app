<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 15px;
      background: #fafafa;
    }

    .screen-header {
      text-align: center;
      color: #7b3f00;
      margin-bottom: 10px;
    }

    .print-header {
      display: none;
      text-align: center;
      margin-bottom: 15px;
    }

    .print-header h2 {
      margin: 0;
      font-size: 20px;
    }

    .print-header p {
      margin: 4px 0 0;
      font-size: 14px;
    }

    .summary {
      margin-bottom: 8px;
      font-size: 16px;
      font-weight: bold;
    }

    .record-info {
      margin-bottom: 12px;
      font-size: 14px;
      font-weight: bold;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      background: white;
    }

    th, td {
      border: 1px solid #000;
      padding: 8px;
      text-align: center;
      font-size: 14px;
    }

    th {
      background: #f2f2f2;
    }

    th a {
      color: black;
      text-decoration: none;
    }

    th a:hover {
      text-decoration: underline;
    }

    .actions {
      margin-top: 15px;
      text-align: center;
    }

    .print-btn {
      background: #2e7d32;
      color: white;
      padding: 8px 16px;
      border: none;
      border-radius: 6px;
      cursor: pointer;
    }

    @media print {

      body {
        background: white;
      }

      nav {
        display: none !important;
      }

      .print-header {
        display: block;
      }

      .screen-header,
      .summary,
      .record-info,
      .actions,
      .pagination {
        display: none !important;
      }

      .hide-print {
        display: none !important;
      }

      th {
        background: #eaeaea !important;
        -webkit-print-color-adjust: exact;
      }
    }
  </style>
</head>

<body>

  <!-- 🖥️ SCREEN HEADER -->
  <h2 class="screen-header">📊 கல்வெட்டு நிதி அறிக்கை (₹5000 மேல்)</h2>

  <!-- 🖨️ PRINT HEADER -->
  <div class="print-header">
    <h2>📊 கல்வெட்டு நிதி அறிக்கை</h2>
    <p>₹5000 மேல் வழங்கிய நிதி விவரம்</p>
  </div>

  <!-- SCREEN ONLY -->
  <div class="summary">
    மொத்த தொகை (₹5000 மேல்): ₹ <%= @total_amount %>
  </div>

  <!-- Showing X – Y of Z -->
  <div class="record-info">
    Showing
    <%= @funds.offset_value + 1 %>
    –
    <%= [@funds.offset_value + @funds.limit_value, @funds.total_count].min %>
    of
    <%= @funds.total_count %>
    records
  </div>

  <!-- 📄 TABLE -->
  <table>
    <thead>
      <tr>
        <th>வ.எண்</th>
        <th>பெயர்</th>
        <th>ஊர்</th>

        <th>
          <%= link_to "தொகை (₹)",
            reports_kalvettu_path(
              sort: "amount",
              direction: params[:direction] == "asc" ? "desc" : "asc"
            ) %>
        </th>

        <th class="hide-print">பணம் செலுத்தும் முறை</th>
        <th class="hide-print">தேதி</th>
      </tr>
    </thead>

    <tbody>
      <% if @funds.any? %>
        <% @funds.each_with_index do |fund, index| %>
          <tr>
            <td><%= @funds.offset_value + index + 1 %></td>
            <td><%= fund.name %></td>
            <td><%= fund.area %></td>
            <td><strong>₹ <%= fund.amount %></strong></td>
            <td class="hide-print"><%= fund.payment_mode.capitalize %></td>
            <td class="hide-print"><%= fund.created_at.strftime("%d-%m-%Y") %></td>
          </tr>
        <% end %>
      <% else %>
        <tr>
          <td colspan="6">₹5000 மேல் எந்த பதிவும் இல்லை</td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <div class="pagination">
    <%= paginate @funds %>
  </div>

  <div class="actions">
    <button class="print-btn" onclick="window.print()">🖨️ Print Kalvettu Report</button>
  </div>

</body>
</html>
