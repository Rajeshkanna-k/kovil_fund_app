<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <style>
    body {
      margin: 0;
      font-family: system-ui, -apple-system, BlinkMacSystemFont;
      background: #fafafa;
    }

    .header {
      background: #7b3f00;
      color: white;
      padding: 12px;
      text-align: center;
      font-size: 18px;
      font-weight: bold;
    }

    .container {
      padding: 16px;
    }

    .title {
      text-align: center;
      color: #7b3f00;
      margin-bottom: 25px;
      font-size: 24px;
    }

    .cards {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(230px, 1fr));
      gap: 18px;
      max-width: 1000px;
      margin: auto;
    }

    .card {
      padding: 20px;
      border-radius: 10px;
      background: white;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
      text-align: center;
    }

    .fund {
      background: #fff3e0;
      border-left: 6px solid #ff9800;
    }

    .expense {
      background: #ffebee;
      border-left: 6px solid #f44336;
    }

    .remaining {
      background: #e8f5e9;
      border-left: 6px solid #4caf50;
    }

    .amount {
      font-size: 22px;
      font-weight: bold;
      margin-top: 8px;
    }

    .fund .amount { color: #e65100; }
    .expense .amount { color: #c62828; }
    .remaining .amount { color: #2e7d32; }

    .actions {
      margin-top: 30px;
      display: flex;
      flex-wrap: wrap;
      gap: 12px;
      justify-content: center;
    }

    .btn {
      padding: 10px 18px;
      border-radius: 6px;
      text-decoration: none;
      color: white;
      font-size: 14px;
    }

    .btn.primary { background: #7b3f00; }
    .btn.dark { background: #444; }
    .btn.success { background: #2e7d32; }

    @media (max-width: 480px) {
      .title {
        font-size: 20px;
      }

      .header {
        font-size: 16px;
      }
    }
  </style>
</head>

<body>

  <!-- 🔝 HEADER -->

  <div class="container">

    <h1 class="title">கோவில் நிதி மேலாண்மை</h1>

    <!-- 📊 SUMMARY -->
    <div class="cards">

      <div class="card fund">
        <h3>மொத்த நிதி</h3>
        <div class="amount">
          ₹ <%= @total_fund %>
        </div>
      </div>

      <div class="card expense">
        <h3>செலவு</h3>
        <div class="amount">
          ₹ <%= @total_expense %>
        </div>
      </div>

      <div class="card remaining">
        <h3>மீதமுள்ள தொகை</h3>
        <div class="amount">
          ₹ <%= @remaining_fund %>
        </div>
      </div>

    </div>

    <!-- 🔗 ACTION BUTTONS -->
    <div class="actions">
      <%= link_to "➕ நிதி சேர்க்க", new_fund_collection_path, class: "btn primary" %>
      <%= link_to "📄 நிதி பட்டியல்", fund_collections_path, class: "btn dark" %>
      <%= link_to "📜 கல்வெட்டு அறிக்கை", reports_kalvettu_path, class: "btn success" %>
    </div>

  </div>

</body>
</html>
